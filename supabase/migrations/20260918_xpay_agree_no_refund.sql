-- ============================================================
-- 订单增加「不退款须知勾选时间」（微信虚拟支付争议举证材料）
--
-- 背景：微信虚拟支付的用户投诉仲裁要求商户举证
-- 「用户购买前已显著知晓并不退款规则且主动确认」。
-- 端上在用户勾选确认框时记录时间戳，随下单请求上送并落库。
-- 服务端强校验：缺该字段的下单请求直接 400，保证无法绕过确认流程。
-- ============================================================

ALTER TABLE xpay_orders ADD COLUMN IF NOT EXISTS agree_no_refund_at TIMESTAMPTZ;
